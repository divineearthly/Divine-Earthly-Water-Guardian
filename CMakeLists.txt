cmake_minimum_required(VERSION 3.10)\nproject(VedicKernel)\nset(CMAKE_CXX_STANDARD 17)\nset(CMAKE_CXX_STANDARD_REQUIRED ON)\nadd_library(vedic_kernel SHARED cpp/vedic_kernel.cpp)\ntarget_include_directories(vedic_kernel PRIVATE cpp/)\nif(MSVC)\ntarget_compile_options(vedic_kernel PRIVATE /O2 /W4)\nelse()\ntarget_compile_options(vedic_kernel PRIVATE -O3 -Wall -Wextra)\endif()\nset_target_properties(vedic_kernel PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib")\ninstall(TARGETS vedic_kernel LIBRARY DESTINATION lib RUNTIME DESTINATION bin)\ninstall(FILES cpp/vedic_kernel.h DESTINATION include)